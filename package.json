{
  "name": "multi-metadata",
  "version": "1.0.1",
  "main": "dist/index.js",
  "author": "bin",
  "keywords": [
    "nft",
    "metadata",
    "image",
    "multicall"
  ],
  "description": "fetch NFT metadata (name, description, image) in batches from contract storage, support multiple chains",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist/* && webpack --progress",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bindoteth/multi-metadata.git"
  },
  "bugs": {
    "url": "https://github.com/bindoteth/multi-metadata/issues"
  },
  "homepage": "https://github.com/bindoteth/multi-metadata#readme",
  "dependencies": {
    "axios": "^1.2.2",
    "ethers": "^5.7.2",
    "web3": "^1.8.1"
  },
  "devDependencies": {
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
